using [UI]
using [UI].Texts
using [UI].Layout
using [UI].Widgets
using [UI].Styles
using [UI].Elements
using [CORE]
using [CORE].Actions

[PATH]: IsVBox, AlignFill, PaddingNone {
    (UIOf, [TARGET])
    Item: IsHBox, AlignFill, PaddingNone {
        Label: ElementItemLabel {Text: {"[TEXT]"}}
    }
    Container: IsVBox, AlignFill, PaddingItem {}
}