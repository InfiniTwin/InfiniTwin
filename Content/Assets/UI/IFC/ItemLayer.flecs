using [UI]
using [UI].Texts
using [UI].Layout
using [UI].Widgets
using [UI].Styles
using [UI].Elements
using [CORE]
using [CORE].Actions
using [IFC]

[PATH]: IsHBox, FillWidthOne, AlignCenterLeft, PaddingNormal {
    (UIOf, [TARGET])
    OrderedChildren
    ToggleState: ElementToggleVisibility {Enabled}
    Label: ElementItemLabel {
        Order: {1}
        Text: {"[NAME]"}
    }
}