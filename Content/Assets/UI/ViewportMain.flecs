using [SCOPE].Layout
using [SCOPE].Element

prefab [SCOPE].IsToolbar : IsCompoundWidget {Toolbar}

[SCOPE].ViewportMain {
    Viewport
    ToolbarTopLeft : IsToolbar {
        Container : IsHBox {
            WindowToggle : ChipButton, AlignTopLeft, PaddingButton {
                Container.Icon {Code: {"e6fa"}}
                Container.Text {
                    Toggle
                    LocalizedText: {"IT::Window"}
                    Order: {1}
               }
           }
       }
   }
}