using [UI].Layout
using [UI].Widgets
using [UI].Elements

[UI].ViewportMain {
    Viewport
    ToolbarTopLeft: IsCompoundWidget {
        Container: IsHBox {
            WindowToggle: ChipButton, AlignTopLeft, PaddingButton {
                Container.Icon {[UI].Icon: {"e6fa"}}
                Container.Text {
                    LocalizedText: {"IT::Window"}
                    Order: {1}
               }
           }
       }
   }
}