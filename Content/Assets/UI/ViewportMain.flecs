using [UI].Layout
using [UI].Element

prefab [UI].IsToolbar : IsCompoundWidget {Toolbar}

[UI].ViewportMain {
    Viewport
    ToolbarTopLeft : IsToolbar {
        Container : IsHBox {
            WindowToggle : ChipButton, AlignTopLeft, PaddingButton {
                Container.Icon {Code: {"e6fa"}}
                Container.Text {
                    Toggle
                    LocalizedText: {"IT::Window"}
                    Order: {1}
               }
           }
       }
   }
}