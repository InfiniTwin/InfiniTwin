using [UI].Layout
using [UI].Elements

[UI].ViewportMain {
    Viewport
    ToolbarTopLeft : IsCompooundWidget {
        Container : IsHBox {
            WindowToggle : ChipButton, AlignTopLeft, PaddingButton {
                Container.Icon {[UI].Icon: {"e6fa"}}
                Container.Text {
                    LocalizedText: {"IT::Window"}
                    Order: {1}
               }
           }
       }
   }
}