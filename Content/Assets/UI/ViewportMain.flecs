using [UI].Layout
using [UI].Element

prefab [UI].IsToolbar {
    CompoundWidget
    Toolbar
}

[UI].ViewportMain {
    Viewport
    ToolbarTopLeft : IsToolbar {
        Container {
            HBox
            WindowToggle : ChipButton, AlignTopLeft, PaddingButton {
                Container.Icon {[UI].Icon: {"e6fa"}}
                Container.Text {
                    LocalizedText: {"IT::Window"}
                    Order: {1}
               }
           }
       }
   }
}